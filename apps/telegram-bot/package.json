{
  "name": "realsfinder-telegram-bot",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check api/health.js && node --check api/webhook.js && node --check scripts/set-webhook.mjs"
  }
}
