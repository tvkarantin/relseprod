import AiDashboardApp from './App'

/**
 * Page wrapper that mounts the high-fidelity AI Reels dashboard.
 * Imported from PR #3 (https://github.com/tvkarantin/relseprod/pull/3)
 * and adapted to live inside the existing `apps/web` project.
 */
export function AiDashboardPage() {
  return <AiDashboardApp />
}
